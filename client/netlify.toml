[build]
  command = "npm run build"
  publish = "dist"

# Fallback para React Router (redundante com public/_redirects, garante no painel)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
